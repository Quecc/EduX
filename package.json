{
  "name": "BearEdu",
  "version": "1.0.0",
  "private": true,
  "description": "BearEdu / EduX local web app with secure Gemini proxy server",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  }
}